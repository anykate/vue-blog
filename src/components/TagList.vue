<template>
  <div class="tag-list">
    <h3>Tags</h3>
    <div v-for="tag in tags" :key="tag">
      <router-link :to="{ name: 'Tag', params: { tag } }">
        #{{ tag }}
      </router-link>
    </div>
  </div>
</template>

<script>
import useTags from '@/actions/useTags'
export default {
  props: {
    posts: {
      type: Array
    }
  },
  setup(props) {
    const { tags } = useTags(props.posts)
    return {
      tags
    }
  }
}
</script>

<style scoped>
.tag-list {
  padding: 10px;
}
.tag-list h3 {
  border-bottom: 1px solid #eee;
  padding: 16px 8px;
  color: #444;
}
.tag-list div {
  display: inline-block;
  padding: 10px;
}
.tag-list a {
  color: #ccc;
  text-decoration: none;
}
.tag-list a.router-link-active {
  color: blueviolet;
  font-weight: bold;
}
</style>